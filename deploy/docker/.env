# --- Variables de entorno para despliegue con Docker Compose ---
# Copia este archivo a .env y ajusta valores para tu entorno.

# PostgreSQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=fullstackhero

# Puertos publicados en el host
API_HOST_PORT=7000
BLAZOR_HOST_PORT=7100
POSTGRES_HOST_PORT=5533

# URLs pÃºblicas (ajusta a dominio/HTTPS en producciÃ³n)
API_BASE_URL=http://localhost:${API_HOST_PORT}/
BLAZOR_PUBLIC_URL=http://localhost:${BLAZOR_HOST_PORT}

# Seguridad
# Genera una clave fuerte para JWT (Base64 o string largo aleatorio)
JWT_KEY=VkrsoT52a/K2C+KtXiXTbqN7ce5Su4Xs3C0QKiYTSiAzTAm82ljO6pOVRu9/a+a5obT/LmpHYBILvx6GnuSgpQ==

# CORS / Origin (ajusta a tu dominio)
# Puedes aÃ±adir mÃºltiples AllowedOrigins usando Ã­ndices __0, __1, etc. en compose.
CORS_ALLOWED_ORIGIN_0=http://localhost:${BLAZOR_HOST_PORT}
CORS_ALLOWED_ORIGIN_1=

# Redis opcional (si lo habilitas en la API mÃ¡s adelante)
REDIS_CONNECTION=
