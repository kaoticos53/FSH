# --- Variables de entorno para despliegue con Docker Compose ---
# Copia este archivo a .env y ajusta valores para tu entorno.

# PostgreSQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=fullstackhero

# Puertos publicados en el host
API_HOST_PORT=7000
BLAZOR_HOST_PORT=7100
POSTGRES_HOST_PORT=5533

# URLs públicas (ajusta a dominio/HTTPS en producción)
API_BASE_URL=http://localhost:${API_HOST_PORT}/
BLAZOR_PUBLIC_URL=http://localhost:${BLAZOR_HOST_PORT}

# Seguridad
# Genera una clave fuerte para JWT (Base64 o string largo aleatorio)
JWT_KEY=PLEASE_CHANGE_ME_TO_A_STRONG_SECRET

# CORS / Origin (ajusta a tu dominio)
# Puedes añadir múltiples AllowedOrigins usando índices __0, __1, etc. en compose.
CORS_ALLOWED_ORIGIN_0=http://localhost:${BLAZOR_HOST_PORT}
CORS_ALLOWED_ORIGIN_1=

# Redis opcional (si lo habilitas en la API más adelante)
REDIS_CONNECTION=

# --- Traefik (opcional) ---
# Si usas el override docker-compose.traefik.yml, define los hosts y el email de Let's Encrypt
# API_HOST=api.tu-dominio.com
# BLAZOR_HOST=app.tu-dominio.com
# LETSENCRYPT_EMAIL=tu-email@dominio.com
